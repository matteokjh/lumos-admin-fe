import React from "react";
import "./chart.sass"

const Chart1 = (props: any) => {

    const { option } = props

    return (
        <div className="chart1 chart">
            <h2>文章统计</h2>
        </div>
    );
};

export default Chart1;
